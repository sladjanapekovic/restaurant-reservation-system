{
  "name": "restaurant-reservation-system",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test:frontend": "jest -c jest.frontend.config.js --coverage",
    "test:backend": "jest -c jest.backend.config.js --coverage",
    "test": "npm run test:frontend && npm run test:backend"
    "build:frontend": "rm -rf dist && mkdir dist && cp index.html app.js style.css dist/",
    "build:backend": "rm -rf backend/build && mkdir -p backend/build && cp backend/*.js backend/package.json backend/build/"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "jest-environment-jsdom": "^29.7.0",
    "supertest": "^7.0.0",
    "@testing-library/jest-dom": "^6.6.3"
  }
}
